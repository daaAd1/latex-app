'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeInTheDOM = toBeInTheDOM;

var _jestMatcherUtils = require('jest-matcher-utils');

var _utils = require('./utils');

function toBeInTheDOM(received) {
  var _this = this;

  if (received) {
    (0, _utils.checkHtmlElement)(received, toBeInTheDOM, this);
  }
  return {
    pass: !!received,
    message: function message() {
      return [(0, _jestMatcherUtils.matcherHint)(`${_this.isNot ? '.not' : ''}.toBeInTheDOM`, 'element', ''), '', 'Received:', `  ${(0, _jestMatcherUtils.printReceived)(received ? received.cloneNode(false) : received)}`].join('\n');
    }
  };
}